services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dcp
      POSTGRES_PASSWORD: dcp
      POSTGRES_DB: dcp
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
