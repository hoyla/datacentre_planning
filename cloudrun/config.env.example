# Copy to cloudrun/config.env (gitignored) and fill in. This repository is
# public, which is why the project id is not committed — same convention as
# the EdgeOne deployment URL.
#
# The ${PROJECT:-…} form means an explicit PROJECT=<id> on the command line
# still overrides the value here.
PROJECT="${PROJECT:-your-gcp-project-id}"
